{
  "name": "englishapp",
  "private": true,
  "version": "1.0.0",
  "description": "Tutor de inglés — frontend React + API FastAPI",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev"
  }
}
